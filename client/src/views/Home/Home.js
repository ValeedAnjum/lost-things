import React, { Fragment } from "react";
import Items from "../../components/Items/Items";

const Home = () => {
  return (
    <Fragment>
      <Items />
    </Fragment>
  );
};

export default Home;
